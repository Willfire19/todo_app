<header>
	<nav class="navbar navbar-default navbar-fixed-top navbar-inverse" role="navigation">
		<div class="container-fluid">
			<!-- Brand and toggle get grouped for better mobile display -->
			<div class="navbar-header">
				<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
					<span class="sr-only">Toggle navigation</span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	      </button>
				<%= link_to "TodoRaider", home_path, id: "logo" %>
				
			</div>
			<!-- Collect the nav links for toggling -->
			
			<div class="collapse navbar-collapse">

				<ul class="nav navbar-nav navbar-right">
					<li class="<%= 'active' if current_page?(home_path) %>"><%= link_to "Home", home_path %></li>
					<% if signed_in? %>
						<li class="<%= 'active' if current_page?(current_user) %>"><%= link_to "Profile", current_user %></li>
						<!-- <li class="<%= 'active' if current_page?(edit_user_path(current_user)) %>"><%= link_to "Edit Profile", edit_user_path(current_user) %></li> -->
						<!-- <li class="<%= 'active' if current_page?(new_user_todo_path(current_user)) %>"><%= link_to "New Todo", new_user_todo_path(current_user) %></li> -->
						<li><%= link_to "Sign out", signout_path, method: "delete" %></li>
					<% else %>
						<li class="<%= 'active' if current_page?(signin_path) %>"><%= link_to "Sign in", signin_path %></li>
					<% end %>
				</ul>
			</div>
		</div>
	</nav>
</header>